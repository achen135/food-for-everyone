# build output
.next/
out/
coverage/
node_modules/

# generated
next-env.d.ts
package-lock.json

# vault-authored master copies (edited in Obsidian, not here)
CLAUDE.md
docs/

# supabase CLI local state (gitignored, but present after `supabase link`)
supabase/.temp/

# generated vendor copy (scripts/copy-maplibre-worker.mjs)
public/maplibre/

# ml subsystem: Python source (ruff formats it) plus a virtualenv and generated
# corpus output that Prettier has no business walking. The two Markdown files
# that ARE prose — ml/README.md and ml/docs/simulator.md — are re-included
# below, since they sit alongside the TypeScript docs in review.
ml/.venv/
ml/out/
ml/src/
ml/tests/
